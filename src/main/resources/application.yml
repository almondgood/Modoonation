spring:
  h2:
    console:
      enabled: true
      path: /h2-console

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:test
    username: sa
    password:

server:
  port: 8080
  servlet:
    context-path: /

logging:
  level:
    web: debug
    root: info
    sql: debug
